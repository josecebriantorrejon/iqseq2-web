export function initContactForm() {
    const contactForm = document.getElementById('contactForm');
    if (!contactForm) return;
    // Permitir envío nativo del formulario al servicio configurado en action
} 